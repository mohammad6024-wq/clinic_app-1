// Preload script to bridge Electron APIs safely to the renderer if necessary.
window.addEventListener('DOMContentLoaded', () => {
  console.log('Fatemi Clinic Electron shell initialized successfully.');
});
